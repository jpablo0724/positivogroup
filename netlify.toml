[build]
  command = "npm run build"
  publish = "dist"

# Vite 8 requiere Node 20.19+; se fija la versión para no depender
# del valor por defecto de Netlify.
[build.environment]
  NODE_VERSION = "22"

# La app es una SPA: cualquier ruta debe servir index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
